{# empty Twig template #}
<div>
  <a href="{{ path('opm_traduction',{'langue':'fr'})}}"><img src="{{ asset('bundles/OpmTest/images/fr.png') }}"></a>
   <a href="{{ path('opm_traduction',{'langue':'en'})}}"><img src="{{ asset('bundles/OpmTest/images/en.png') }}"></a>
{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
    <a href="{{ path('fos_user_profile_show') }}">{{app.user.username|upper }}</a> |
    <a href="{{ path('fos_user_security_logout') }}">
       
       {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
    </a>
    {% endif %}
</div>

{% for key, flash in app.session.getFlashes() %}
<div class="{{ flash }}">
    {{ key|trans([], 'FOSUserBundle') }}
</div>
{% endfor %}
